name: environment-variable
on: 
  push:
    branches: [master]
jobs:
  environment-variable:
    runs-on: ubuntu-latest
    env:
      LEVEL: job-level-env
    steps:
      - name: print the env
        run: echo $LEVEL


     